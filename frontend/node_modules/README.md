## Node Modules

The `node_modules` folder contains all the dependencies installed via `npm` (Node Package Manager) for the frontend React app.

**Important:**

- This folder is **not committed** to the repository because it can be regenerated.
- To install the required Node.js packages, run the following command in the frontend directory:

```bash
npm install
